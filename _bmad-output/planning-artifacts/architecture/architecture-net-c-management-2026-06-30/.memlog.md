---
scope: Rebrand + payment-mode + UI/UX refresh of net-c-management (Next.js 16 brownfield); architecture invariants across PRD features 4.1-4.4 (FR-1..15)
purpose: build-substrate
altitude: feature
updated: 2026-06-30T18:01
---

- (decision) Paradigm = layered server-first Next.js App Router monolith (ratified from code). Server Components read via server-only src/lib helpers. ALL mutations via REST Route Handlers under src/app/api. Single Prisma client + single service-role Supabase client. Cross-cutting: auth.ts/proxy.ts, i18n dictionary, zod validation.
- (decision) AD-1 Mutation boundary: all writes go through Route Handlers; src/lib and Server Components are read-only; no Next Server Actions introduced. Prevents two divergent mutation paradigms. ADOPTED-existing.
- (decision) AD-2 Auth/authz contract per route: await auth -> 401; isAdminRole(role) -> 403 for admin mutations (never role == ADMIN literal; OWNER must pass); zod safeParse -> 400 {error,details}. Enforced twice (proxy.ts + layout guard). ADOPTED.
- (constraint) AD-3 Activity(ekskul)-scoping security invariant: member reads scoped by getUserEkskulIds; member mutations gated by assertMembership. Every new per-session/payment query stays ekskul-scoped. Prevents cross-ekskul data leak. ADOPTED.
- (decision) AD-4 Payment is the single money model. Extend it: add enum PaymentType {MONTHLY, SESSION} and nullable sessionId (FK ActivitySession). NO separate SessionPayment model. Reuses proofUrl/proofPath/status/confirmedBy/confirmedAt. Prevents two payment owners and divergent proof+confirm flows.
- (decision) AD-5 Payment uniqueness: unique(userId, sessionId) for SESSION rows (null sessionId on monthly rows never collides in Postgres). Monthly uniqueness (userId, ekskulId, month, year) becomes a PARTIAL unique index WHERE type = MONTHLY via raw SQL; drop the unconditional unique (it would block multiple per-session payments in one month). Prevents month/year collision across session charges.
- (decision) AD-6 Per-session billing trigger = pre-pay-on-register (UX override of pay-after-attend). A per-session member slot is SECURED at proof upload (session Payment >= PENDING). Attendance REGISTERED for a per-session member requires a matching session Payment; cancel/reject releases the slot. Monthly members register free. Prevents free slot-holding and ambiguous secured-owner. ASSUMPTION: secured at upload (PENDING) not admin-confirm, per UX spine.
- (decision) AD-7 Membership owns the member chosen payment mode (single source, NOT inferred from latest Payment). Mode change effective NEXT billing period; current period mode immutable. v1: paymentMode enum column on Membership applied going-forward. Prevents mid-period mode flip changing dues. ASSUMPTION: single column v1; if strict per-period history needed add effective-dated record.
- (decision) AD-8 Ekskul(Activity) owns ALL fees + allowed modes: monthlyFee (single source; rename defaultFee->monthlyFee, ASSUMPTION optional), new sessionFee (ActivitySession.fee defaults from it), allowsMonthly + allowsPerSession booleans (at least one true, enforced in zod + route). Global Settings.defaultMonthlyFee REMOVED. Activity fee explicit-required (no accidental 0). Binds FR-7/8/9/15.
- (decision) AD-9 Rename BadmintonSession -> ActivitySession (accessor prisma.activitySession; relations Ekskul.sessions, Attendance.session; /api/sessions routes; @prisma/client types). MUST NOT be plain Session (NextAuth collision). Lands BEFORE payment-mode work (PRD sequencing). ADOPTED FR-6.
- (decision) AD-10 Identity is data not hardcode. Brand from Settings.communityName (neutral defaults en Sports Community / id Komunitas Olahraga) + communityAbbr fallback; no bundled logo; neutral favicon. Zero badminton/PB-Net-C user-facing strings; Ekskul label -> Activity/Aktivitas (model name unchanged); all strings via dictionary, en/id parity. ADOPTED FR-1/2/3/4.
- (decision) AD-11 UI refresh only - no new design system / component lib; reuse shadcn + Tailwind v4 + next-themes. Desktop-first base + sm:/md: adapt-down; member screens fully mobile-usable, admin desktop-first. Tokens inherited from UX spine: accent Deep Teal 0F766E light / 2DD4BF dark, WCAG 2.2 AA, Tailwind default breakpoints, member max-w-2xl, admin tables -> stacked cards under md. ADOPTED from UX.
- (decision) AD-12 Schema evolution: prisma generate + prisma db push (pre-launch, no prod data, no migration files). Partial unique index for monthly Payment applied via raw SQL / prisma db execute since db push cannot express it. No fee backfill (pre-launch); fee made explicit-required instead.
- (constraint) Companion UX spine ux-net-c-management-2026-06-30 (DESIGN.md/EXPERIENCE.md) is binding for visual tokens and flows: pre-pay-on-register flow, Deep Teal accent, Tailwind breakpoints, WCAG 2.2 AA, member max-w-2xl.
- (version) Stack from package.json (reality-checked, not training-data): Next.js 16.2.6, React 19.2.4, TypeScript 5, Prisma 7.8.0 + @prisma/adapter-pg + driverAdapters preview, pg 8.20.0, NextAuth 5.0.0-beta.31, Tailwind CSS 4, shadcn 4.7 / radix, zod 4.4.3, react-hook-form 7.75.0, supabase-js 2.105.3.
- (question) Open/deferred: exact paymentMode column shape (single vs effective-dated per-period); whether to rename defaultFee->monthlyFee; exact partial-index SQL for monthly Payment uniqueness; neutral favicon asset choice. UX already resolved OQ2 (switch next period), OQ3 (pre-pay on register), OQ4 (no fee backfill).
- (event) Fast-path draft: 12 ADs + paradigm logged from PRD + UX + brownfield code sweep (schema.prisma, settings.ts, supabase.ts, payments + attendance routes, ekskul.ts). Distilling spine next.
- (event) Reviewer gate: lint_spine clean (0). Version reviewer MINOR (Stack table fixed: shadcn 4.7 is CLI only; added radix/lucide/sonner/next-themes + @hookform/resolvers versions; dropped driverAdapters preview wording - GA since 6.15). Divergence reviewer MANY HOLES (9 holes + 4 silent dimensions) - all in the new payment-mode section. Applying fixes.
- (decision) AD-4 tightened: SESSION Payment derives month/year from ActivitySession.date (so month filters/stats include per-session); ekskulId = session.ekskulId (preserves AD-3 scoping); amount is server-derived from the session fee, NEVER trusted from client (security). Closes divergence H1/H8/H9.
- (decision) AD-5 tightened (resolves AD-4/AD-5 write-pattern contradiction H2): SESSION rows use prisma.payment.upsert on native @@unique([userId,sessionId]). MONTHLY rows use a partial unique index WHERE type=MONTHLY applied via raw SQL; monthly writes use a transactional insert-or-update (raw ON CONFLICT or guarded txn), NOT prisma.payment.upsert (cannot target a partial index). Existing monthly upload upsert is migrated to this pattern.
- (decision) AD-6 tightened (H4/H5): per-session registration is owned by the payment upload route, which creates the SESSION Payment (PENDING) + Attendance (REGISTERED) atomically in ONE prisma.$transaction, after a successful Supabase proof upload. Capacity authority = Attendance count (REGISTERED/PRESENT), single source. The free /api/sessions/[id]/attendance route rejects per-session members (payment-required). REJECT (PATCH /api/payments/[id]) or member cancel releases the seat (deletes the paired Attendance). Binds extended to the reject + attendance-DELETE paths.
- (decision) AD-7 tightened (H3): billing period = canonical calendar (month 1-12, year) per AD-13. A member effective mode for a period is a function of period via effectiveFrom (YYYYMM), NOT a bare mutable column - a switch sets effectiveFrom = next month and never changes how the current period resolves (current period immutable). Single-row-with-pending vs effective-dated history table stays a deferred impl detail; the period-function invariant is pinned.
- (decision) AD-8 tightened (H6/H7): Payment.amount SNAPSHOTS the applicable fee at charge creation; later Activity fee edits never retroactively change existing Payment amounts. New/future charges read the live Activity fee. Disabling an allowed mode is NOT retroactive - current-period members keep their mode and must switch for next period (ties AD-7).
- (decision) AD-13 NEW: canonical billing-period primitive = calendar (month: 1-12, year: int). Single shared definition referenced by AD-5/6/7/8; closes the calendar-month vs join-anniversary divergence.
- (decision) AD-14 NEW: transaction & compensation policy. Any money write touching more than one row runs in one prisma.$transaction (e.g. SESSION Payment + Attendance). Supabase storage upload precedes the DB transaction; an orphaned storage object on txn failure is accepted pre-launch (no compensation/cleanup job in v1). Closes the silent transaction/compensation dimension.
- (event) Spine updated with tightened AD-4..AD-8 + new AD-13/AD-14; AD IDs stable. Re-distill + close next.
- (event) spine finalized (status: final). 14 ADs; lint clean; version + divergence gates applied. Build substrate ready; feeds bmad-create-epics-and-stories.
