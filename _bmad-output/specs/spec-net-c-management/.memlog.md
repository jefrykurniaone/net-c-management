---
topic: net-c-management pre-launch productization: activity-agnostic rebrand, per-Activity fees + member payment modes, UI/UX refresh (FR-1..15), distilled from ARCHITECTURE-SPINE
updated: 2026-06-30T18:17
---

- (event) create op; input ARCHITECTURE-SPINE.md (structured, pre-sorted); express distill - input rich, no elicitation
- (decision) ARCHITECTURE-SPINE.md + UX DESIGN.md + project-context.md = adopted companions (downstream reads); prd.md + addendum.md = absorbed sources
- (decision) 5 capabilities: PRD features 4.1-4.4 with rename FR-6 broken out as own cap - different risk/sequencing per PRD sec 6 and AD-9
- (capability) CAP-1 activity-agnostic rebrand FR-1..5: neutral configurable branding, zero badminton/PB-Net-C user-facing strings, en/id parity
- (capability) CAP-2 BadmintonSession to ActivitySession codebase rename FR-6; name not plain Session (NextAuth collision); lands before payment work
- (capability) CAP-3 per-Activity fee + payment-mode config FR-7..9: monthlyFee + sessionFee + allowsMonthly/allowsPerSession (>=1 true); global default fee removed
- (capability) CAP-4 member payment-mode selection + billing FR-10..12: pick offered mode; monthly flat/month; per-session per session joined
- (capability) CAP-5 UI/UX refresh + full responsiveness + Settings IA FR-13..15: desktop-first, members fully mobile-usable, one home per setting
- (constraint) AD-1 single mutation boundary - all writes via Route Handlers /api; no Server Actions; src/lib read-only, never imports src/app
- (constraint) AD-3 Activity(ekskul) data scoping is security invariant - no cross-ekskul leak; member reads scoped, mutations assertMembership
- (constraint) AD-2 per-route auth/authz - auth()->401, isAdminRole->403 (never role===ADMIN, OWNER passes), zod->400; page guarded twice proxy+layout
- (constraint) AD-4/AD-5 single Payment money model - extend with type+sessionId; no separate SessionPayment; mode-partitioned uniqueness
- (constraint) AD-8 Activity owns all fees+allowed modes; amount snapshots at charge; global Settings.defaultMonthlyFee removed; fee explicit-required
- (constraint) AD-7 Membership owns payment mode, resolved per billing period; switch sets effectiveFrom=next period, current period immutable
- (constraint) AD-6/AD-14 per-session pre-pay-on-register - one prisma. creates SESSION Payment PENDING + REGISTERED Attendance; capacity=Attendance count
- (constraint) AD-11 no new UI dep/design system - reuse shadcn+Tailwind v4+next-themes; desktop-first, members fully mobile; tokens from UX spine; WCAG 2.2 AA
- (constraint) AD-10 brand+Activity identity is data not hardcoded; all user-facing strings via i18n dictionaries en/id parity
- (constraint) AD-12 schema via prisma generate + db push (no migration files, pre-launch); monthly partial unique index via raw SQL
- (assumption) paymentMode v1 shape = paymentMode + effectiveFrom(YYYYMM) + pending value on Membership; richer effective-dated history deferred (AD-7)
- (assumption) member may switch payment mode between periods, effect next period (AD-7 resolves PRD Open Q2)
- (assumption) defaultFee->monthlyFee rename optional/cosmetic; single-source invariant holds either way (AD-8)
- (assumption) responsive breakpoints follow standard Tailwind scale sm/md/lg/xl (FR-13, AD-11)
- (question) confirm per-session slot secured at proof upload (Payment PENDING) not at admin-confirm - architecture assumed per UX spine (AD-6)
- (question) desktop-first layout vs predominantly phone-based members - revisit before launch? (PRD Open Q1)
- (event) SPEC.md derived: 5 capabilities (CAP-1..5), 13 constraints, 9 non-goals, success signal, 4 assumptions, 2 open questions; architecture+UX+project-context as adopted companions, prd+addendum as sources
- (event) self-validate pass 1 coherence: PASS - all caps have intent+success; intents are WHAT; constraints all bend design; 9 non-goals; success signal concrete/testable; IDs unique; prose lean
- (event) self-validate pass 2 preservation: PASS - PRD FR-1..15 mapped to CAP-1..5; SM-1..5 in success criteria; AD-1..14 + conventions + stack + diagrams preserved (Constraints distill load-bearing ones, full set in architecture companion); non-goals + assumptions + open questions carried; no load-bearing drop
